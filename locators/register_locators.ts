export const RegisterLocators = {
  // Fields
  emailInput: 'input[name="email"]',
  passwordInput: 'input[name="password"]',
  confirmPasswordInput: 'input[name="confirm_password"]',

  // Buttons
  submitButton: 'button[type="submit"]',

  // Flash messages
  flashSuccess: '.flash-success',
  flashDanger: '.flash-danger',

  // Field validations
  emailInvalid: 'input[name="email"]:invalid',
  passwordInvalid: 'input[name="password"]:invalid'
};
